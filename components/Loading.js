import Wrapper from './Wrapper';

const Loading = React.createClass({
  render() {
    return (
      <Wrapper>
        <p>
          Loading...
        </p>
      </Wrapper>
    );
  }
});

export default Loading;
